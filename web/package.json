{
    "private": true,
    "name": "escad_webclient",
    "version": "0.1.0",
    "description": "This is a html web application for a client that talks with escad to provide graph tools.",
    "scripts": {
	"test": "echo \"Error: no test specified\" && exit 1",
	"build": "esbuild escad-client.js --bundle --minify --sourcemap --platform=browser --target=chrome58,firefox57,safari11,edge16 --outfile=bundle.js --analyze"
    },
    "dependencies": {
	"cytoscape": "^3.21.0",
	"cytoscape-context-menus": "^4.1.0",
	"xstate": "^4.30.6"
    },
    "devDependencies": {
	"esbuild": "^0.14.25"
    },
    "author": "Markus Kollmar",
    "license": "GPL-3.0-or-later"
}
